{
  "name": "xinmiao-network-check",
  "version": "1.6.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "description": "XinMiao Network Check - Cloudflare Worker full-stack static assets + API"
}
